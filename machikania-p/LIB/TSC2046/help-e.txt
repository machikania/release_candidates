<Class name and version>
TSC2046
ver 0.3

<File name>
TSC2046.BAS

<License>
The BAS file is in the public domain.

<Overview>
A class for reading touch‑position data from an LCD with a touch panel using the TSC2046. Note that the update from ver. 0.2.1 to ver. 0.3 includes specification changes. Up to ver. 0.2.1, static methods and fields were used, but starting with ver. 0.3, the design has been changed to use instance fields and methods instead.

<Constructor>
Argument 1
Specify the GP (General Purpose) pin number connected to the CS pin for SPI communication. If omitted, the default is 3. For the Waveshare ResTouch, specify 9.
Argument 2
Specify the GP pin number connected to the IRQ pin of the TSC2046. If omitted, the default is 0. For the Waveshare ResTouch, specify 10.

<Public Fields>
Touch
	Set to 1 when the panel is being touched, otherwise 0.

Xpos
	Holds the X-axis touch position in pixels.

Ypos
	Holds the Y-axis touch position in pixels.

<Public Methods>
POLLING()
	Checks whether a touch is present and updates the fields Touch, Xpos, and Ypos. The return value is the same as the Touch field.

MINMAX()
	Displays a calibration window to compensate for device-specific variations and saves the results to the /LIB/TSC2046 directory. This method is automatically called right after installing the TSC2046 class, so it does not need to be run every time.

<Public Static Fields>
These are provided only for compatibility with ver. 0.2.1 and are deprecated.

TSC2046::lastx
Stores the x-axis value of the most recent touch reading.
TSC2046::lasty
Stores the y-axis value of the most recent touch reading.

<Public Static Methods>
These are provided only for compatibility with ver. 0.2.1 and are deprecated.

TSC2046::PORTRAIT()
	Sets the LCD to portrait orientation.

TSC2046::LANDSCAPE()
	Sets the LCD to landscape orientation.

<Usage Example>
For the Waveshare ResTouch, change line 6 to "t=new(TSC2046,9,10)".

useclass TSC2046

cls
x=-1

t=new(TSC2046,3,0)
rem t.MINMAX()

do
  if t.POLLING() then
    if 0<=x then
      line x,y,t.Xpos,t.Ypos
    else
      pset t.Xpos,t.Ypos
    endif
    x=t.Xpos
    y=t.Ypos
  endif
  if 0=t.Touch then x=-1
  delayms 1
loop
